import { ScrollView } from 'react-native-gesture-handler';
import { View, Text } from '../components/Themed';

export default function TabTwoHorizontalScreen(props) {
  return (
    <ScrollView>
      <Text>Left to right</Text>
    </ScrollView>
  );
}
